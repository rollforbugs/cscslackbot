debug_mode = boolean(default=True)
plugin_dir = string(default='plugins')
plugins = string_list(default=list())

[logging]
time_format = string(default='%Y-%m-%d %H:%M:%S')

[commands]
prefix = string(max=10, default='!')
